# MongoDB Configuration
# Utilisez soit la méthode 1 (URI complet) soit la méthode 2 (composants séparés)

# Méthode 1: URI MongoDB complet (RECOMMANDÉ)
# MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/tamagocho-db?retryWrites=true&w=majority

# Méthode 2: Composants MongoDB séparés (ACTUELLEMENT UTILISÉ)
MONGODB_USERNAME=votre_nom_utilisateur
MONGODB_PASSWORD=votre_mot_de_passe
MONGODB_HOST=votre_cluster.mongodb.net
MONGODB_DATABASE_NAME=tamagocho-db
MONGODB_PARAMS=?retryWrites=true&w=majority
MONGODB_APP_NAME=Tamagocho

# Better Auth - GitHub OAuth (optionnel)
GITHUB_CLIENT_ID=votre_github_client_id
GITHUB_CLIENT_SECRET=votre_github_client_secret

# Application URL (pour production)
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Better Auth Secret (générez-en un avec: openssl rand -base64 32)
BETTER_AUTH_SECRET=votre_secret_aleatoire_tres_long

# Better Auth URL
BETTER_AUTH_URL=http://localhost:3000
